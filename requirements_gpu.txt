# GPU (CUDA) requirements for RTX 4090
torch>=2.0.0
transformers>=4.40.0
datasets>=2.20.0
peft>=0.11.0
accelerate>=0.30.0
bitsandbytes>=0.43.0

# Flash attention for faster training (GPU only)
flash-attn>=2.5.0

# Core dependencies
requests>=2.31.0
jsonlines>=4.0.0
tqdm>=4.66.0
numpy>=1.24.0
pandas>=2.0.0

# Optional for enhanced functionality
tensorboard>=2.13.0
wandb>=0.15.0